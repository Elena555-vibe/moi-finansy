# Спецификация MVP: личные финансы

**Версия:** 1.0  
**Статус:** готово к передаче агенту-разработчику  
**Язык интерфейса:** русский  
**Целевая среда:** iPhone / Safari, вертикальная ориентация, устанавливаемое PWA

## 1. Назначение продукта

Личное мобильное веб-приложение для одного пользователя, которое помогает ежедневно фиксировать доходы и расходы, соблюдать месячный лимит и формировать личную копилку.

Продукт не является универсальной домашней бухгалтерией. Его ключевой вопрос: **«Сколько я ещё могу безопасно потратить в этом месяце?»**

### Цели MVP

- Быстро и вручную фиксировать фактические доходы и расходы.
- Показывать доступный остаток и остаток лимита на текущий месяц.
- Планировать ежемесячный резерв, отслеживать фактически отложенную сумму и прогресс до цели копилки.
- Учитывать будущие платежи как напоминания.
- Показывать структуру расходов по категориям.
- Работать локально и без подключения к интернету, а при наличии сети синхронизировать личную серверную копию.

### Критерий успеха продукта

После месяца использования пользователь понимает фактическое распределение трат, укладывается в установленный лимит и увеличивает личную копилку.

## 2. Продуктовые принципы

1. **Минимум полей.** Для обычной операции нужны только сумма, дата и категория/источник.
2. **Фактические деньги прежде всего.** Главный экран прежде всего показывает общий остаток и свободные деньги; месячный лимит доступен во вкладке «Копилка».
3. **Фактические и плановые деньги разделены.** Плановый доход не изменяет реальный баланс; резерв, который уже фактически отложен, уменьшает свободные деньги.
4. **Все действия обратимы.** Операции можно редактировать и удалять; показатели пересчитываются сразу.
5. **Мобильный приоритет.** Интерфейс должен быть понятен и удобен на iPhone одной рукой.

## 3. Объём MVP

### Входит в MVP

- Первоначальная настройка: начальный остаток, категории, план текущего месяца.
- Копилка: планируемый доход, плановый резерв, автоматически вычисляемый месячный лимит и целевая сумма копилки.
- Операции: расход, доход, пополнение копилки, изъятие из копилки, «Взяла в долг», «Вернула долг» и будущий платёж (напоминание).
- Редактирование и удаление операций.
- Главный экран с ключевыми показателями и диаграммой расходов.
- Список операций, сгруппированный по датам.
- Управление категориями, источниками дохода и копилкой.
- Офлайн-first хранение, PWA, работа без сети и фоновая синхронизация с личным сервером.

### Не входит в MVP

- Интеграции с банками, импорт выписок, распознавание чеков.
- Банковские интеграции, экспорт и ручное резервное копирование.
- Семейный бюджет, несколько счетов и валют. Допускаются до 20 независимых личных пользователей без доступа к данным друг друга.
- Копейки, подкатегории, комментарии к расходам и другим операциям, названия операций, файлы и чеки. Исключение: у дохода допустим необязательный комментарий.
- Автоматический расчёт процентов, графики платежей и несколько накопительных целей.
- Лимиты по категориям, сравнение месяцев, расширенная аналитика.
- Push-уведомления, PIN-код, биометрия, автоматическая блокировка.
- Полноценная десктопная версия и переключатель светлой/тёмной темы.

## 4. Роли и хранение данных

| Параметр | Решение |
|---|---|
| Пользователь | До 20 независимых личных пользователей; у каждого собственная учётная запись и полностью изолированная история |
| Авторизация | E-mail и пароль; постоянная сессия на доверенном устройстве |
| Хранилище | IndexedDB на устройстве + личная серверная база данных |
| Рекомендуемая реализация | IndexedDB — рабочая офлайн-копия; API и PostgreSQL/MySQL — постоянная серверная копия |
| Синхронизация | Автоматическая после каждой локальной записи при наличии сети |
| Офлайн-режим | Обязателен; изменения ставятся в очередь синхронизации |

> При первом входе на существующем устройстве приложение обязано предложить перенести локальные данные на сервер. Оно не должно очищать или заменять локальную историю без явного подтверждения пользователя.

Открытая регистрация допускается, пока серверный лимит не достигнут. Лимит — 20 аккаунтов суммарно, включая первоначальный; сервер должен обеспечивать его транзакционно и возвращать понятное сообщение для следующего пользователя. В интерфейсе регистрации отображается число оставшихся личных копий. Аккаунты не образуют семейный бюджет и не могут читать, изменять или видеть данные друг друга.

## 5. Информационная архитектура и навигация

Нижняя навигация состоит ровно из четырёх разделов:

| Раздел | Назначение |
|---|---|
| Главная | Текущее финансовое состояние и распределение расходов |
| Операции | История доходов, расходов, движений копилки и напоминаний |
| Копилка | План текущего календарного месяца и управление личными накоплениями |
| Настройки | Начальный остаток, категории и источники дохода |

На всех основных экранах доступна плавающая кнопка **«+»**. Она открывает меню выбора: **Расход**, **Доход**, **В копилку**, **Будущий платёж**.

```text
Главная → «+» → тип операции → полноэкранная форма → «Сохранить» → Главная
```

## 6. Финансовая модель и расчёты

### 6.1 Термины

| Термин | Определение |
|---|---|
| Начальный остаток | Сумма денег на дату начала учёта, введённая при первом запуске |
| Планируемый доход | Ожидаемая общая сумма доходов месяца; не является фактическим поступлением |
| Плановый резерв | Сумма, которую пользователь планирует отложить в копилку в текущем месяце |
| В копилке | Сумма операций «В копилку» за вычетом операций «Из копилки» |
| Целевая сумма | Общая сумма, которую пользователь хочет накопить в копилке |
| Обычный расход | Расход, уменьшающий повседневный месячный лимит |
| Из копилки | Возврат накопленных средств в свободные деньги; не является расходом |
| Будущий платёж | Напоминание о предполагаемом расходе; до оплаты не влияет на расчёты |

### 6.2 Базовые формулы

Все суммы хранятся и рассчитываются в целых рублях.

| Показатель | Формула |
|---|---|
| Месячный лимит | `планируемый доход − плановый резерв` |
| Осталось потратить | `месячный лимит − сумма обычных расходов периода` |
| Общий остаток | `начальный остаток + все фактические доходы − все фактические расходы` |
| Свободные деньги | `общий остаток − фактически отложено в резерв` |
| Осталось накопить до цели | `целевая сумма − сумма в копилке` |

**Сумма в копилке** рассчитывается как:

`сумма операций «В копилку» − сумма операций «Из копилки»`.

Планируемый доход и плановый резерв не меняют общий остаток: они используются для планирования лимита. Обычные расходы включают, в частности, продукты, транспорт, коммунальные платежи, подписки и связь.

### 6.3 Допустимые отрицательные значения

Сохранение не блокируется, если отрицательными становятся общий остаток, свободные деньги, остаток лимита или фактический резерв. Приложение отражает реальное состояние. При отрицательных итогах на конец месяца отображается внутреннее предупреждение.

### 6.4 Периоды

- Финансовый месяц совпадает с календарным: с 1-го по последний день месяца.
- Месяц закрывается автоматически по календарю; отдельная кнопка закрытия не нужна.
- Операции прошлых месяцев доступны для свободного редактирования и удаления.
- Любое изменение пересчитывает все зависимые показатели соответствующего и текущего периодов.
- Неиспользованный лимит не переносится отдельной механикой на следующий месяц. Следующий лимит всегда вычисляется из нового плана месяца.

## 7. Операции и правила CRUD

### 7.1 Типы операций

| Тип | Поля создания | Влияние на общий остаток | Влияние на лимит |
|---|---|---:|---:|
| Расход | сумма, категория, дата | уменьшает | уменьшает |
| Доход | сумма, источник, дата, необязательный комментарий | увеличивает | не влияет |
| В копилку | сумма, дата | не влияет | не влияет |
| Будущий платёж | сумма, категория, дата | не влияет | не влияет |
| Из копилки | сумма, дата | не влияет | не влияет |
| Взяла в долг | сумма, дата | увеличивает | не влияет |
| Вернула долг | сумма, дата | уменьшает | не влияет |

### 7.2 Расход

- Поля в форме: **сумма → категория → дата → Сохранить**.
- Сегодняшняя дата подставляется по умолчанию, но её можно изменить календарём на прошлую или текущую дату.
- Если пользователь намерен указать будущую дату, он создаёт тип «Будущий платёж», а не фактический расход.
- После сохранения осуществляется переход на Главную.
- Расход можно изменить (сумма, категория, дата) и удалить через меню «⋯».
- Перед удалением обязательно показывать подтверждение, например: «Удалить операцию на 2 500 ₽?».

### 7.3 Доход

- Поля: сумма, источник дохода, дата.
- Учитываются только фактически поступившие доходы; будущие ожидаемые доходы отдельными операциями не создаются.
- Доход можно изменить и удалить; расчёты обновляются немедленно.

### 7.4 Резерв

- Поля: сумма и дата.
- Добавление резерва увеличивает фактически отложенную сумму, но не уменьшает общий остаток.
- Резерв можно изменить и удалить так же, как доход и расход.
- Разрешено добавить резерв больше текущего общего остатка; свободные деньги могут стать отрицательными.

### 7.5 Будущий платёж

- Поля: сумма, категория, дата.
- Это напоминание, а не расход: до подтверждения не меняет баланс, свободные деньги и лимит.
- За один календарный день до даты платежа на Главной и/или в Операциях показывается внутреннее предупреждение.
- В строке будущего платежа отображается статус **«Ожидается»** и стандартные кнопки действий: меню `⋯` и удаление. Отдельной кнопки «Оплачено» нет.
- В меню `⋯` доступны: **«Подтвердить оплату»**, **«Редактировать»** и **«Удалить»**. «Подтвердить оплату» открывает форму расхода с предзаполненными суммой, категорией и датой; сумму и дату можно поправить.
- После сохранения создаётся фактический расход, а напоминание остаётся в истории со статусом **«Оплачено»**.

### 7.6 Изъятие из копилки

На экране управления копилкой пользователь вводит сумму и дату изъятия. После подтверждения:

1. создаётся операция «Из копилки»;
2. сумма в копилке уменьшается;
3. свободные деньги увеличиваются;
4. общий остаток и повседневный месячный лимит не изменяются.

Изъятие разрешено и при недостаточной сумме в копилке: показатель может стать отрицательным, чтобы честно отразить ситуацию.

### 7.7 Долговые обязательства

Во вкладке «Копилка» ниже управления копилкой отображается блок «Долговые обязательства»:

- **К возврату** = первоначальный долг из старой версии (если он был указан) + сумма операций «Взяла в долг» − сумма операций «Вернула долг»;
- **Возвращено** = сумма операций «Вернула долг»;
- кнопка «Управлять долговыми обязательствами» открывает лист с этими показателями и действиями «Взяла в долг» и «Вернула долг»;
- каждая операция содержит только сумму и дату, подлежит редактированию и удалению в истории;
- заёмные деньги увеличивают общий и свободный остаток, но не учитываются в доходах и не меняют месячный лимит;
- возврат уменьшает общий и свободный остаток, а также «К возврату», но не изменяет месячный лимит и не уменьшает сумму в копилке автоматически;
- отрицательный остаток долга разрешён и честно отображается.

В MVP ведётся один общий долг: без кредитора, процентной ставки, графика платежей и напоминаний о долге.

### 7.8 Минимальная техническая валидация

Не вводить сложных финансовых запретов. Однако форма не должна сохранять технически неполную запись: сумма должна быть числом больше нуля, а обязательные поля (дата и категория/источник, где применимо) должны быть заполнены. Пользователь исправляет остальные ошибки через редактирование или удаление.

## 8. Модель данных

Идентификаторы — UUID. Временные метки создания и изменения хранятся отдельно от пользовательской даты операции.

### 8.1 Сущности

| Сущность | Ключевые поля |
|---|---|
| `AppSettings` | `initialBalance`, `initialBalanceDate`, `locale`, `currency`, `onboardingCompleted` |
| `MonthlyPlan` | `id`, `yearMonth`, `plannedIncome`, `plannedReserve`, `monthlyLimit` (вычисляемый или кэшированный), `createdAt`, `updatedAt` |
| `Savings` | `targetAmount`, `createdAt`, `updatedAt` |
| `Category` | `id`, `name`, `icon`, `sortOrder`, `isArchived` |
| `IncomeSource` | `id`, `name`, `sortOrder`, `isArchived` |
| `Transaction` | `id`, `type`, `amount`, `date`, `categoryId?`, `incomeSourceId?`, `createdAt`, `updatedAt` |
| `FuturePayment` | `id`, `amount`, `date`, `categoryId`, `status` (`planned` / `paid`), `createdAt`, `updatedAt` |

### 8.2 Типы `Transaction.type`

`expense` | `income` | `reserve` | `savings_withdrawal`

### 8.3 Требования к целостности данных

- Архивированная категория или источник не доступны при создании новых операций, но сохраняются у исторических записей.
- При удалении категории с историей она **архивируется**, а не удаляется физически.
- При редактировании или удалении операций пополнения и изъятия детерминированно пересчитываются сумма в копилке и свободные деньги.
- Дата хранится как локальная календарная дата (`YYYY-MM-DD`), без смещения UTC. Отображение: `ДД.ММ.ГГГГ`.

## 9. Экраны

### 9.1 Онбординг / первоначальная настройка

Последовательность:

1. Ввести начальный остаток и дату начала учёта.
2. Проверить или настроить категории.
3. Создать план текущего месяца: планируемый доход и плановый резерв.
4. Перейти на Главную. Целевую сумму копилки можно указать позднее.

### 9.2 Главная

Экран показывается **без прокрутки** на целевых экранах iPhone. Главный элемент — тёмно-оливковая карточка с двумя визуально разделёнными показателями:

- общий остаток;
- свободные деньги.

Также отображаются:
- горизонтальные цветные шкалы расходов текущего месяца по категориям с суммой по каждой категории, без процентов.

Не показывать на Главной последние операции, расширенную аналитику, календарь, сравнение периодов или дополнительные информационные блоки.

### 9.3 Операции

- История группируется по датам.
- Доходы отмечены знаком `+`, расходы — знаком `−`.
- В строке дохода заголовком является выбранный источник дохода, а необязательный комментарий отображается вторичным текстом рядом с датой.
- Пополнения и изъятия копилки, а также будущие платежи имеют различимые подписи/иконки и статусы.
- Детальные фильтры и поиск не входят в MVP.
- Меню `⋯` у будущего платежа открывает: подтверждение оплаты, редактирование и удаление. У фактических операций оно открывает редактирование; удаление доступно отдельной кнопкой с подтверждением.

### 9.4 Копилка

Минимальный экран текущего месяца:

| Поле/показатель | Поведение |
|---|---|
| Планируемый доход | Редактируется вручную |
| Плановый резерв | Редактируется вручную |
| Рекомендуемый месячный лимит | Вычисляется автоматически |
| В копилке | Вычисляется из операций пополнения и изъятия |
| Целевая сумма | Редактируется вручную в управлении копилкой |
| Осталось накопить | `целевая сумма − сумма в копилке` |
| Остаток лимита | `месячный лимит − обычные расходы` |
| К возврату | `первоначальный долг (если есть) + взяла в долг − вернула долг` |
| Возвращено | Сумма операций «Вернула долг» |

План допускается изменять в течение месяца; вычисления должны обновляться сразу.

### 9.5 Управление копилкой

Отображает и позволяет изменить:

- целевую сумму;
- текущую сумму в копилке;
- остаток до цели;
- сумму и дату операции «Изъять из копилки».

### 9.6 Настройки

Доступно изменить:

- начальный остаток;
- категории: создать, переименовать, изменить иконку, порядок, архивировать;
- источники доходов: создать, переименовать, изменить порядок, архивировать;
- данные первоначальной настройки при необходимости.

Кнопки «Удалить все данные», экспорта и выбора темы нет.

## 10. Справочники

### Стартовые категории расходов

- Жильё
- Продукты
- Бытовые товары
- Кафе
- Транспорт
- Связь и интернет
- Здоровье
- Красота
- Одежда и обувь
- Образование
- Развлечения
- Цифровые сервисы
- Подарки
- Путешествия и отпуск
- Прочее

В настройках и на Главной категория отображается с небольшой иконкой из дизайн-системы и названием. Категории доступны при внесении расхода и будущего платежа. Последняя выбранная категория не запоминается.

### Стартовые источники доходов

- Зарплата
- Самозанятость
- Проценты по вкладу
- Прочее

Источник «Возврат» в MVP отсутствует.

Пользователь может добавить собственный источник дохода через поле «Новый источник дохода» и кнопку «Добавить» в Настройках.

## 11. PWA и офлайн

- Приложение должно быть устанавливаемым PWA с иконкой и manifest.
- После первой успешной загрузки основные ресурсы приложения должны быть доступны без сети через service worker.
- Все операции должны создаваться, редактироваться и отображаться офлайн.
- IndexedDB остаётся рабочей локальной копией. При наличии сети изменения синхронизируются с личным сервером.
- Доступ к серверной копии выполняется по e-mail и паролю; пароль пользователя передаётся только по HTTPS и хранится на сервере в виде хеша.
- Первый вход на устройстве с существующими данными должен предлагать безопасно перенести их на сервер. Локальные записи не удаляются автоматически.
- При восстановлении серверной истории на новом устройстве приложение обязано запрашивать подтверждение до замены локальной копии.
- Приложение должно корректно открываться из иконки на домашнем экране iPhone без необходимости пользоваться адресной строкой Safari.

## 12. Мобильные UX- и доступностные требования

- Целевая ориентация — вертикальная.
- Приоритетная ширина — типичные iPhone; минимально поддерживаемая CSS-ширина — 320 px.
- Интерфейс не должен иметь горизонтальную прокрутку.
- Формы добавления и редактирования открываются полноэкранно и корректно учитывают safe area iPhone.
- Экранная клавиатура не должна закрывать активное поле или кнопку сохранения.
- Минимальная интерактивная область — **44 × 44 px**.
- Все основные действия доступны пальцем и, где возможно, одной рукой.
- Текст остаётся читаемым, контраст не строится исключительно на цвете.
- Цвета и стили реализуются через дизайн-токены: визуальная система будет предоставлена отдельно.

## 13. Локаль, даты и деньги

- Только русский язык; переключатель языка отсутствует.
- Валюта — российский рубль (`₽`).
- Копейки не используются.
- Отображение денег: `125 400 ₽` (пробел как разделитель тысяч).
- Локальная дата берётся из часового пояса устройства; пользователь может вручную выбрать нужную дату операции.
- Названия месяцев — на русском.

## 14. Нефункциональные требования

- Данные сохраняются между перезапусками приложения и браузера, пока пользователь не очищает хранилище Safari.
- Интерфейс и расчёты обновляются сразу после любого CRUD-действия, без ручного обновления страницы.
- Приложение не требует подключения к интернету для основной функциональности.
- Ошибка одного некорректного пользовательского ввода не должна приводить к потере уже сохранённых данных.
- Использовать простую, поддерживаемую архитектуру: изолированный доменный слой расчётов и UI-компоненты, не содержащие дублирующих финансовых формул.
- Все вычисления сумм выполнять целыми рублями; не использовать float для денежной арифметики.

## 15. Реализационные заметки для агента

- Создать единый модуль/сервис селекторов расчётов: баланс, свободные деньги, фактический резерв, лимит, остаток лимита, категоризацию расходов. UI должен получать уже вычисленные значения из него.
- Все списки и диаграмма должны фильтроваться по календарному месяцу выбранной даты операции.
- Расходы будущих периодов нельзя создавать как `expense`: направлять пользователя к сущности будущего платежа.
- Операция «Из копилки» не должна считаться расходом: она уменьшает копилку и увеличивает свободные деньги, но не меняет баланс и лимит.
- По возможности хранить транзакции как источник истины, а сумму в копилке вычислять детерминированно. Это упрощает корректный перерасчёт после редактирования истории.
- Логику дат тестировать на границе месяцев, високосного года и при смене часового пояса устройства.
- Не добавлять неутверждённые функции под видом «задела»: MVP сознательно минимален.

## 16. Приёмочные критерии MVP

### Функциональная готовность

- [ ] При первом запуске можно пройти настройку начального остатка, категорий и плана месяца.
- [ ] План месяца автоматически рассчитывает лимит как `планируемый доход − плановый резерв`.
- [ ] Можно добавить, изменить и удалить расход; диаграмма, баланс и лимит пересчитываются сразу.
- [ ] Можно добавить, изменить и удалить доход; общий остаток пересчитывается сразу.
- [ ] Можно добавить, изменить и удалить пополнение копилки; свободные деньги и сумма в копилке пересчитываются сразу.
- [ ] Можно создать будущий платёж с суммой, категорией и датой.
- [ ] За день до будущего платежа виден внутренний reminder.
- [ ] Будущий платёж имеет статус «Ожидается», его можно изменить или удалить, а через меню `⋯` — подтвердить оплату, изменить предзаполненную сумму/дату и создать из него фактический расход.
- [ ] Оплаченное напоминание сохраняется в истории со статусом «Оплачено».
- [ ] Можно задать целевую сумму и изъять деньги из копилки; свободные деньги и сумма в копилке пересчитываются, а общий остаток и месячный лимит не меняются.
- [ ] Можно зафиксировать «Взяла в долг» и «Вернула долг»; общий/свободный остаток и показатели «К возврату» и «Возвращено» пересчитываются, а доходы, копилка и месячный лимит не искажаются.
- [ ] Разрешены отрицательные показатели; операции не блокируются из-за недостатка денег или резерва.
- [ ] На Главной крупно и раздельно отображаются общий остаток и свободные деньги, а также горизонтальные шкалы с суммами категорий. «Осталось потратить» на Главной не отображается.
- [ ] История операций сгруппирована по датам и различает все типы операций.
- [ ] Категории и источники доходов можно создавать, редактировать, упорядочивать и архивировать; история при этом не теряется.
- [ ] Операции прошлого месяца можно редактировать, и зависимые показатели пересчитываются.

### Техническая и UX-готовность

- [ ] Приложение работает в Safari на iPhone в вертикальной ориентации.
- [ ] Его можно установить на главный экран как PWA и открыть без адресной строки.
- [ ] После первой загрузки основные сценарии работают без подключения к интернету.
- [ ] После закрытия и повторного открытия приложения локальные данные не теряются.
- [ ] Пользователь может создать единственную личную учётную запись или войти в неё с другого устройства.
- [ ] После первой явной синхронизации текущая локальная история сохраняется на сервере без удаления с устройства.
- [ ] Новая или изменённая запись, созданная офлайн, синхронизируется после восстановления сети.
- [ ] Серверная история не заменяет локальную без явного подтверждения пользователя.
- [ ] Нет горизонтальной прокрутки; формы полноэкранны и не ломаются при открытой экранной клавиатуре.
- [ ] Все основные интерактивные элементы имеют область нажатия не менее 44 × 44 px.
- [ ] В проекте отсутствуют функции из раздела «Не входит в MVP».

## 17. Возможные следующие итерации

Не реализовывать в MVP, но учитывать как направления развития: экспорт/резервные копии, банковский импорт, несколько счетов, категории с лимитами, сравнение месяцев, накопительные цели и расширенная аналитика.
